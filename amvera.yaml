meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  commands:
    - npm install --legacy-peer-deps
    - npm run build
  artifacts:
    - dist
    - node_modules

run:
  command: npm start
